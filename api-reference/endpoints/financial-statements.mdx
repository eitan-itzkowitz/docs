---
title: Financial Statements
description: Retrieve structured financial statements for an e-commerce business.
openapi: "GET /financial-statements"
---

import { TryIt } from '@mintlify/components';

## Retrieve Financial Statements

Fetch financial statements such as balance sheet, income statement, and cash activity report.

<TryIt />

### **GET** `/financial-statements`

#### 🔹 **Authorizations**
| Name           | Type   | Location | Required | Description |
|---------------|--------|----------|----------|-------------|
| Authorization | string | Header   | Required | Bearer authentication header of the form `Bearer <token>` |

#### 🔹 **Query Parameters**
| Name        | Type   | Required | Description |
|------------|--------|----------|-------------|
| `start_date` | string (YYYY-MM-DD) | Required | Start date of the financial statement period. |
| `end_date` | string (YYYY-MM-DD) | Required | End date of the financial statement period. |
| `type`  | string | Required | Type of statement: `balance-sheet`, `income-statement`, or `cash-activity-report`. |

---

## **Response Schema**
```json
{
  "lines": [
    {
      "id": "d1e3f5b7-38fa-4a2d-9e4d-3c73c9b9eabc",
      "parentId": null,
      "childIds": ["a9c24d72-bc52-46e9-8d8c-108dc856b64f"],
      "depth": 0,
      "name": "Total Revenue",
      "type": "rollup",
      "amountType": "credit",
      "intervals": [
        { "amount": { "amount": 152340.75, "currency": "USD" } },
        { "amount": { "amount": 143220.50, "currency": "USD" } }
      ]
    },
    {
      "id": "b342d28a-3c65-44f5-95e3-19a6a5b53f92",
      "parentId": null,
      "childIds": [],
      "depth": 0,
      "name": "Operating Expenses",
      "type": "rollup",
      "amountType": "debit",
      "intervals": [
        { "amount": { "amount": -84320.00, "currency": "USD" } },
        { "amount": { "amount": -79210.50, "currency": "USD" } }
      ]
    }
  ],
  "properties": {
    "basis": "accrual",
    "currency": "USD",
    "key": "income_statement",
    "name": "Income Statement",
    "intervals": [
      { "key": "current_period", "periods": ["2024-01", "2024-02"] },
      { "key": "year_to_date", "periods": ["2024-Q1"] }
    ]
  }
}

Field Definitions
Financial Statement Structure
Each financial statement is structured into individual lines, representing account groups, accounts, or rollup categories.

Key Properties
<table> <thead> <tr> <th>Field</th> <th>Type</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td><code>id</code></td> <td>string</td> <td>Unique identifier for this statement line.</td> </tr> <tr> <td><code>parentId</code></td> <td>string or null</td> <td>If present, this line belongs to a parent grouping. Null for top-level lines.</td> </tr> <tr> <td><code>childIds</code></td> <td>array of string</td> <td>IDs of child lines under this line.</td> </tr> <tr> <td><code>depth</code></td> <td>integer</td> <td>Depth level in hierarchy. Used for indentation.</td> </tr> <tr> <td><code>name</code></td> <td>string</td> <td>Human-readable label (e.g., "Total Revenue", "Shipping Costs").</td> </tr> <tr> <td><code>type</code></td> <td>enum</td> <td>One of: <code>rollup</code> (aggregated totals), <code>parent_account_group</code> (subgroup of accounts), or <code>account</code> (individual account).</td> </tr> <tr> <td><code>amountType</code></td> <td>enum</td> <td>One of: <code>credit</code> (e.g., revenue) or <code>debit</code> (e.g., expenses).</td> </tr> <tr> <td><code>intervals</code></td> <td>array</td> <td>Reported values for each requested period.</td> </tr> </tbody> </table>
