{
  "openapi": "3.0.3",
  "info": {
    "title": "Puzzle API",
    "version": "1.1"
  },
  "paths": {
    "/v1.0/users": {
      "get": {
        "summary": "Retrieve Users (v1.0)",
        "tags": ["Accounting"],
        "responses": {
          "200": {
            "$ref": "#/components/responses/Success200"
          }
        }
      }
    },
    "/v1.1/users": {
      "get": {
        "summary": "Retrieve Users (v1.1)",
        "description": "Updated version with pagination support.",
        "tags": ["Accounting"],
        "parameters": [
          {
            "name": "page",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "example": 1
            },
            "description": "Page number for paginated results."
          }
        ],
        "responses": {
          "200": {
            "$ref": "#/components/responses/Success200"
          }
        }
      }
    }
  },
  "components": {
    "responses": {
      "Success200": {
        "description": "Successful response",
        "x-status": "live",
        "content": {
          "application/json": {
            "schema": {
              "type": "object",
              "properties": {
                "success": { "type": "boolean", "example": true },
                "data": { "type": "array", "items": { "type": "object" } }
              }
            }
          }
        }
      }
    }
  },
  "tags": [
    { "name": "Accounting", "description": "Accounting-related endpoints" }
  ]
}
